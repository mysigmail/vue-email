const html = ref('')
const vue = ref('')
const subject = ref('')
const text = ref('')
const to = ref('')

export function useState() {
  return {
    html,
    vue,
    subject,
    text,
    to,
  }
}
